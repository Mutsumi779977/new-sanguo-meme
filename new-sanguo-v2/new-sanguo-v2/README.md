# 新三国梗系统 v2.8.4

> **⚠️ 重要声明**  
> 本软件仅供学习研究使用，**禁止**以下行为：  
> - ❌ 未经授权封装为商业 SaaS 服务  
> - ❌ 诱导用户通过第三方服务器传输隐私数据  
> - ❌ 移除原作者署名后重新分发  
>   
> 采用 **AGPL-3.0** 许可证，违规使用将追究法律责任。  
> 正确用法：本地部署，数据保存在自己机器。

模块化架构的梗生成系统，支持可配置匹配算法、可选向量功能、联网搜索增强、话题映射、**信息充足度检测**、**显式搜索**、**日常闲聊**、**单元测试**、**底线梗随机回退机制**、**Token 零消耗优化**。

## 文档

- [快速开始](#快速开始)
- [API文档](API.md) - 完整的 Python API 参考
- [配置说明](config.yaml) - 配置项详细说明

## 快速开始

```bash
# 安装依赖
pip install pyyaml

# 运行
python run.py
```

## 核心特性

- **🚀 Token 零消耗**: 纯本地运行，默认不调用任何 AI 接口
- **🎭 底线梗随机回退**: 匹配失败时优雅兜底，随机选择 + 智能回退
- **可配置匹配**: 权重公式、温度参数均可调整
- **meta 梗融合**: 自动融合称呼类 meta 梗
- **向量匹配**: 可选（默认关闭），支持语义相似度
- **视频转文字录入**: 自动提取有效梗信息
- **联网搜索增强**: 遇到不理解的信息自动搜索后匹配
- **话题映射**: 识别 LCK/科技/娱乐/日常等领域话题，自动匹配合适梗
- **信息充足度检测**: 智能判断是否需要联网搜索背景知识
- **显式搜索**: 用户可主动要求搜索
- **日常闲聊**: 支持吃什么/天气/问候等日常对话

## 信息充足度检测（方案A+）

Agent 自动判断是否有足够信息回答话题：

```python
from new_sanguo import create_agent

agent = create_agent("user_id")
result = agent.handle("用户输入")

# result 可以是字符串（快速回复）或字典（需要搜索）
if isinstance(result, dict) and result.get('type') == 'need_search':
    print(f"需要搜索: {result['reason']}")
    # 执行搜索后再次调用
else:
    print(f"快速回复: {result}")
```

### 检测逻辑

| 场景 | 行为 | 示例 |
|:---|:---|:---|
| 用户要求搜索（显式）| **优先搜索** | "帮我搜索..." / "查一下..." |
| 包含评价性关键词 | need_search | "剧情怎么样" / "如何评价" |
| 话题默认需背景知识 | need_search | 科技发布 / 时政新闻 |
| 本地可处理关键词 | 快速回复 | "抽卡歪了" / "taco行为" |
| 未知话题 | need_search | 未识别内容 |

### 显式搜索（跳过自动判断）

你可以直接要求系统联网搜索，跳过自动判断：

```
用户: 帮我搜索原神诺德卡莱剧情
系统: [触发搜索] → 获取信息后生成梗

用户: 查一下特朗普最新动态
系统: [触发搜索] → 获取信息后生成梗
```

支持的触发词：
- `搜索`、`搜一下`、`帮我搜`
- `查一下`、`查查`、`查查看`
- `去搜`、`去查`、`找一下`

**显式搜索 vs 自动判断对比：**

| 输入 | 自动判断 | 显式搜索 |
|:---|:---|:---|
| "gen2:0T1" | 本地快速回复 | 可强制搜索 |
| "帮我搜索gen2:0T1" | - | 强制搜索 |
| "GPT5发布了" | need_search | need_search |
| "搜索GPT5发布" | - | 强制搜索 |

## 命令

| 命令 | 说明 |
|:---|:---|
| `/录入 [视频]` | 录入新梗 |
| `/查询 <关键词>` | 查询梗库 |
| `/玩梗 <内容>` | 手动触发玩梗 |
| `/称呼 <名称>` | 生成 xx 爷称呼 |
| `/统计` | 查看统计 |
| `/融合 [内容]` | 测试融合功能 |

## 配置

编辑 `config.yaml`:

```yaml
# 温度参数（采样随机性）
matching:
  temperature: 1.0  # <1 更确定，>1 更随机

# 权重公式系数
scoring:
  weights:
    similarity: 0.6      # 语义相似度
    base_weight: 0.3     # 基础权重

# 向量功能（默认关闭）
embedding:
  enabled: false

# 搜索增强
search:
  enabled: true
  trigger_threshold: 0.3
```

## 话题映射示例

### LCK 电竞赛事

```
输入: "gen2:0T1"
识别: LCK赛事 Gen.G 0:1 输给 T1
映射: 震惊、否认、不服输
输出: 不可能，绝对不可能！

输入: "T1 3:0 横扫"
识别: T1 大胜
映射: 得意、嘲讽
输出: 我原本以为吕布已经天下无敌了
```

### 科技话题

```
输入: "GPT5 发布了"
识别: 科技/AI 话题
映射: 震惊、自负
输出: 好方略，不过我想稍作修改
```

### 日常闲聊

```
输入: "我晚上要吃什么呢？"
识别: 日常话题
映射: 无奈、调侃
输出: 是啊，吃什么

输入: "今天天气怎么样"
识别: 日常话题
映射: 随意、轻松
输出: 好火啊，比夷陵之火还好啊

输入: "你好"
识别: 日常话题
映射: 高冷
输出: 来者何人
```

## Token 消耗说明

**纯本地运行：零 Token 消耗（默认）**

| 功能 | Token 消耗 | 说明 |
|------|-----------|------|
| 梗匹配 | **0** | 本地 SQLite + YAML 查询 |
| 梗生成 | **0** | 本地模板渲染 |
| 变体生成 | **0** | 本地规则替换 |
| 底线梗机制 | **0** | 本地随机选择 + 回退机制 |
| 向量匹配 | **0** | 本地模型（默认关闭）|
| 联网搜索 | **~500-1500** | 触发搜索时才消耗 |

**参照物对比：**
- 一次普通对话：~1,000-3,000 tokens
- 一次代码审查：~5,000-15,000 tokens  
- **本系统日常玩梗**：**0 tokens**（无限畅玩）
- 触发搜索时：~500-1500 tokens

## 底线梗随机回退机制

当所有匹配都失败时，采用**随机选择 + 智能回退**策略：

```
随机选一个底线梗
    ↓
需要核心词？
    ├─ 否 → 直接输出（叉出去/拖出去斩了）
    └─ 是 → 尝试提取核心词
              ↓
         成功？ ├─ 是 → 填充输出
                └─ 否 → 重新随机选择（避免死循环）
```

## 项目结构

```
new_sanguo/
├── __init__.py        # 包入口
├── models.py          # 数据模型
├── config.py          # 配置管理
├── database.py        # 数据库层
├── service.py         # 业务逻辑
├── search_adapter.py  # 搜索适配器
├── topic_mapper.py    # 话题映射器
├── agent.py           # Agent主类
└── utils.py           # 工具函数

data/
├── genku.yaml         # 梗数据源
├── fallback_genkus.yaml  # 底线梗配置
└── genku.db           # SQLite 数据库

config.yaml            # 配置文件
run.py                 # CLI 入口
```

## 版本历史

- **v2.8.4**: 底线梗随机回退机制，Token 消耗优化（纯本地零消耗）
- **v2.7.0**: 删除角色模式，统一为纯梗模式；版本号统一；清理废弃文件
- v2.6.0: 版本号统一、低频功能测试、Bug修复
- v2.5.13: 补充单元测试 (pytest, 102 passed)
- v2.5.12: 命令路由优先级修复
- v2.5.11: 补充单元测试 (pytest, 19 passed)
- v2.5.10: 事不过三定律（高频梗防滥用）
- v2.5.9: 静默降权机制
- v2.5.8: 袁术告老还乡梗
- v2.5.7: "是啊，[动词]什么"深度学习
- v2.5.6: 日常闲聊话题类别
- v2.5.5: 显式搜索功能 + 电竞话题修复
- v2.5.4: 方案A+信息充足度检测
- v2.5.2: 话题映射系统（LCK/科技/娱乐）
- v2.5.1: 联网搜索增强
- v2.5: 模块化重构，可配置权重/温度
- v2.4: 视频转文字录入，称呼生成
- v2.3: meta 梗融合功能
- v2.0: 统一架构，SQLite 持久化

## 作者

- **B站**: [梦雨_raining](https://space.bilibili.com/24250060)

## License
[AGPL-3.0](LICENSE) - GNU Affero General Public License v3.0